const { test, experiment, before, afterEach } = exports.lab = require('lab').script();

const { documents } = require('../../../../../src/modules/crm/lib/queries');
const { pool } = require('../../../../../src/lib/connectors/db');

experiment('modules/crm/lib/queries/documents', () => {

});
